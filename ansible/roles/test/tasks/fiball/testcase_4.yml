# Test Case 4   Verify route nexthop change(from not exist to exist)

# Step1 add route wihich nexthop miss
- set_fact:
    static_route: "{{ static_routes.prefix8 }}"
    nh_interface: "{{ dut_if_vlan }}"
    nh_ip_mask: "{{ minigraph_vlan_interfaces|selectattr('attachto', 'match', dut_if_vlan) | map(attribute='subnet') | ipaddr(2) | first}}"
    nh_interface_peer_port: "{{ dut_if_vlan_peer_port_list[0] }}"
    
- set_fact:
    nh_ip: "{{nh_ip_mask|ipaddr('address')}}"

- name: flush arp/neigh on dut if
  shell: ip link set arp off dev {{ nh_interface }} && ip link set arp on dev {{ nh_interface }}
  become: yes

- name: Add static route which nexthop miss.
  shell: vtysh -c "configure terminal" -c "ip route {{ static_route }} {{ nh_ip }}"
  become: yes  

# Step 2 verify route is created
- name: Use show to verify route is inactive
  shell: show ip route {{ static_route }}
  register: result
  become: true

- fail: msg="Route {{ static_route }} shoud be found in show ip route."
  when: '"{{ static_route }}" not in result.stdout'

# Step3 verify ip traffic cannot be forwarded via this route
- name: Generate ptf test params
  set_fact:
    ingress_rif: "{{ dut_if_4 }}"
    dst_ip_addr_list: "{{ [dst_for_static_route] }}"
    dst_port_list: "{{ [nh_interface_peer_port] }}"
    src_port: "{{ dut_if_4_peer_port }}"

- name: verify ip traffic cannot be forwarded via this route
  include: roles/test/tasks/ptf_runner.yml
  vars:
        ptf_test_name: Fiball testcase_4 step3
        ptf_test_dir: ptftests
        ptf_test_path: fiball_test.FiballTest
        ptf_platform_dir: ptftests
        ptf_platform: remote
        ptf_test_params:
          - verbose=True
          - src_port={{ src_port }}
          - router_mac=\"{{ ansible_Ethernet0['macaddress'] }}\"
          - dst_ip_addr_list=\"{{ dst_ip_addr_list }}\"
          - dst_port_list=\"{{ dst_port_list }}\"
          - unexpected_ip_addr_list=\"{{ dst_ip_addr_list }}\"
        ptf_extra_options: "--relax --debug debug --log-file /tmp/fiball.testcase_4.step3.{{ lookup('pipe','date +%Y%m%d_%H%M%S') }}.log"

# Step4 Ping to trigger route nexthop resolve(from not exist to exist)
- name: Add host ip
  shell: ip address add {{ nh_ip_mask }} dev eth{{ nh_interface_peer_port }}
  delegate_to: "{{ ptf_host }}"
  become: yes

- name: Ping from dut to host for trigger arp resolve
  shell: ping {{ nh_ip }} -c 3 -i0.2 -W2
  become: true

#Step5 verify arp entry of route nexthop is correct
- name: Show arp
  shell: show arp
  register: out
  become: true

- fail: msg="{{ nh_ip }} should be resolved"
  when: '"{{ nh_ip }}" not in out.stdout'

#Step6 verify ip traffic can be forwarded via this route
- name: verify ip traffic cannot be forwarded via this route
  include: roles/test/tasks/ptf_runner.yml
  vars:
        ptf_test_name: Fiball testcase_4 step6
        ptf_test_dir: ptftests
        ptf_test_path: fiball_test.FiballTest
        ptf_platform_dir: ptftests
        ptf_platform: remote
        ptf_test_params:
          - verbose=True
          - src_port={{ src_port }}
          - router_mac=\"{{ ansible_Ethernet0['macaddress'] }}\"
          - dst_ip_addr_list=\"{{ dst_ip_addr_list }}\"
          - dst_port_list=\"{{ dst_port_list }}\"
        ptf_extra_options: "--relax --debug debug --log-file /tmp/fiball.testcase_4.step6.{{ lookup('pipe','date +%Y%m%d_%H%M%S') }}.log"


