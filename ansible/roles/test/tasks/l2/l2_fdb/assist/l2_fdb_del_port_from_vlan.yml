- set_fact: port_index="{{item}}"

- set_fact: port_name="{{ item.dev }}"
  when: item.port_index == '{{port_index}}'
  with_items: fdb_ports_list

- name: Del {{port_name}} from Vlan{{vlan_id}}
  shell: config vlan member del {{ vlan_id }} {{ port_name }}
  become: true
  ignore_errors: true
